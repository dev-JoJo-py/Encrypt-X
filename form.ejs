<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>EncryptX Text Console</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #00f5a0;
      --bg: #0e0e10;
      --card-bg: #1a1a1d;
      --text: #e5e5e5;
      --muted: #888;
      --accent: #00dba5;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background-color: var(--bg);
      color: var(--text);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 20px;
    }

    .form-container {
      background: var(--card-bg);
      border-radius: 20px;
      padding: 40px;
      width: 100%;
      max-width: 480px;
      box-shadow: 0 20px 50px rgba(0, 245, 160, 0.05);
      border: 1px solid rgba(0, 245, 160, 0.2);
      animation: fadeIn 1s ease;
    }

    h2 {
      margin-bottom: 20px;
      font-size: 22px;
      font-weight: 600;
      text-align: center;
      color: var(--primary);
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    input, textarea {
      padding: 14px 16px;
      border: none;
      border-radius: 12px;
      background-color: #121214;
      color: var(--text);
      font-size: 14px;
      transition: box-shadow 0.3s;
    }

    input:focus, textarea:focus {
      outline: none;
      box-shadow: 0 0 0 2px var(--primary);
    }

    textarea {
      resize: vertical;
      min-height: 140px;
    }

    button {
      background: var(--primary);
      color: #0e0e10;
      padding: 12px;
      border: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: 15px;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: var(--accent);
    }

    @keyframes fadeIn {
      0% {
        transform: translateY(10px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>EncryptX - Secure Text</h2>
    <form action="/code" method="post">
      <textarea placeholder="Paste your text here..." name="data" required></textarea>
      <input type="text" name="code" placeholder="Enter a code for retrieval..." required />
      <input type="text" name="time" placeholder="Expiry time in minutes (default 60)..." />
      <button type="submit">Encrypt Text</button>
    </form>
  </div>
</body>
</html>
